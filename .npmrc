# Configuração para evitar aviso do npm sobre --production
# Força instalação de todas as dependências (incluindo devDependencies) durante o build
# O Railway usa NODE_ENV=production, mas precisamos das devDependencies para o build do Vite
